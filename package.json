{
  "name": "schoolmanager-pro",
  "version": "1.0.0",
  "private": true,
  "description": "SchoolManager Pro — plateforme multi-établissements (API + frontend)",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "dev:server": "cd server && npm install && npm start",
    "dev:client": "cd client && npm install && npm run dev",
    "db:init": "psql \"$DATABASE_URL\" -f db/schema.sql"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.0",
    "pdfkit": "^0.15.0",
    "pg": "^8.12.0",
    "xlsx": "^0.18.5"
  }
}
